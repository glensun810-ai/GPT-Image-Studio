# ======================================================================
# GPT Image Studio - 环境变量示例
# ======================================================================
# 复制本文件为 .env.local 并填入真实值即可（不要提交 .env.local）
#
#   cp .env.example .env.local
#
# 本地运行只需要 JWT_SECRET 这一个变量，其他都有合理默认值。
# ======================================================================

# --- 必填（推荐本地也设上，避免重启后 cookie 失效） -----------------------
# 用于签发/校验登录会话（HS256），任意强随机字符串即可
# 可用命令生成：openssl rand -base64 32
JWT_SECRET=replace-me-with-a-long-random-string-min-32-chars

# --- 可选：调整服务监听端口 ----------------------------------------------
# 默认 5000；如果本机 5000 已被占用，可改成其他端口
# PORT=5000

# --- 可选：节点环境标识 ---------------------------------------------------
# 本地开发保持 development 即可；线上部署（coze / Vercel / 自建）设为 production
# NODE_ENV=development

# --- 关于 apimart.ai API Key ----------------------------------------------
# API Key 不放在环境变量里。
# 原因：每个用户有自己的 Key；用户在前端"API Key"输入框里填入，
#       由前端保存到 localStorage，提交生成时由浏览器 Header 透传到后端，
#       再由后端用同样的 Key 调 apimart。
# 这意味着：本服务本身不存任何 API Key，部署到任何环境都安全。
